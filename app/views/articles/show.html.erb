<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<%= link_to "<< Back to the Articles List", articles_path %>
<%= link_to "delete", articles_path(@article), method: :delete, data: {confirm: "Do you really want to do this?"}%>
<%= link_to "edit", edit_article_path(@article)%>
